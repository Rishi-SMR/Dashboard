{
  "name": "smr-striven-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Backend that holds Striven API credentials and feeds the SMR dashboard (AR / AP / accounts).",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js"
  }
}
